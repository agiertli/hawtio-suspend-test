# camel-k: language=yaml

# Write your routes here, for example:
- route:
    id: anton-to-behaim-its
    from:
      uri: timer:yaml
      parameters:
        period: "1000"
      steps:
        - setBody:
            expression:
              simple:
                expression: Hello Camel from ${routeId}
        - log: ${body}
